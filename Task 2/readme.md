# 📝 To-Do List Manager (Python)

A simple command-line **To-Do List Manager** built using Python.  
It allows you to **add**, **view**, and **remove** tasks, with all tasks saved in a `tasks.txt` file.

---

## 🔧 Features
- Add new tasks  
- Remove existing tasks  
- View all saved tasks  
- Tasks persist using a text file (`tasks.txt`)

---

## ▶️ How to Run
```bash
python todo.py
```

## 📂 File Structure

```bash
todo.py
tasks.txt   # Auto-created if it doesn't exist
```

## 💡 Description

This script uses basic file handling and a simple menu-based loop to manage tasks directly from the terminal.

